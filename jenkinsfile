pipeline {

    agent any
    
    stages {
        
        stage("build") {
        c
            step {
               echo "builiding the application..." 
            }
        }
        
        stage("test") {
        
            step {
               echo "testing the application..."
            }
        }
        
        stage("deploy") {
        
            step {
               echo "deploy the application..."
            
            }
        }
    }
}
